{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run check:migrations && npm run db:migrate:status -- --process-env && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
